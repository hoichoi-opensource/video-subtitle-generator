# Video Subtitle Generation System Configuration
# Copy this file to config.yaml and update with your settings

# Google Cloud Configuration
google_cloud:
  project_id: "your-project-id"
  location: "us-central1"
  credentials_path: "credentials/service-account.json"
  bucket_name: "your-subtitle-bucket"
  
# Vertex AI Configuration
vertex_ai:
  model: "gemini-2.5-pro-preview-05-06"  # DO NOT CHANGE - This is the required model
  generation_config:
    max_output_tokens: 8192
    temperature: 0.2
    top_p: 0.95
  safety_settings:
    - category: "HARM_CATEGORY_HATE_SPEECH"
      threshold: "OFF"
    - category: "HARM_CATEGORY_DANGEROUS_CONTENT"
      threshold: "OFF"
    - category: "HARM_CATEGORY_SEXUALLY_EXPLICIT"
      threshold: "OFF"
    - category: "HARM_CATEGORY_HARASSMENT"
      threshold: "OFF"

# Processing Configuration
processing:
  chunk_duration: 60  # seconds
  parallel_workers: 3
  retry_attempts: 3
  retry_delay: 5  # seconds
  timeout_per_chunk: 300  # seconds
  
# Supported Languages
languages:
  - code: "en"
    name: "English"
    flag: "🇬🇧"
  - code: "hi"
    name: "Hindi"
    flag: "🇮🇳"
  - code: "bn"
    name: "Bengali"
    flag: "🇧🇩"

# Path Configuration
paths:
  input_dir: "input"
  output_dir: "output"
  temp_dir: "temp"
  logs_dir: "logs"
  
# Naming Conventions
naming:
  chunk_pattern: "{video_name}_{language}_chunk_{number:03d}.mp4"
  subtitle_pattern: "{video_name}_{language}_chunk_{number:03d}.srt"
  final_subtitle: "{video_name}_{language}_subtitled.srt"
  final_video: "{video_name}_{language}_subtitled.mp4"
  job_id_pattern: "{video_name}_{language}_{timestamp}"
  
# Stage Configuration
stages:
  enable_all: true
  stage_timeout: 600  # seconds per stage
  show_progress: true
  
# Logging Configuration
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file_rotation: true
  max_file_size: "10MB"
  backup_count: 5
  
# Output Configuration
output:
  create_preview: true
  burn_subtitles: false  # Whether to burn subtitles into video
  subtitle_formats: ["srt", "vtt"]  # Supported formats
  keep_chunks: false  # Keep video chunks after processing
  
# Performance Configuration
performance:
  max_file_size: "2GB"  # Maximum input file size
  min_disk_space: "10GB"  # Minimum required disk space
  memory_limit: "4GB"  # Memory limit for processing